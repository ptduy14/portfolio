// M2 placeholder — rebuilt as full Agent CLI in M4 (wires askAssistant + function-calling)
export default function TerminalApp() {
  return (
    <div className="flex h-full flex-col bg-term-bg p-4 font-mono text-[13px] text-term-fg">
      <pre className="text-term-purple">duy@portfolio · RAG Agent v1.0</pre>
      <p className="mt-2 text-text-dim">— Agent CLI terminal arrives in M4 —</p>
    </div>
  );
}
